const mainUrl = "http://localhost:9090/api";

export const bookUrlApi = mainUrl + "/books";

export const userUrlApi = mainUrl + "/users";

export const paymentApi = mainUrl + "/payment";

export const podcastApi = mainUrl + "/podcasts";

export const videoApi = mainUrl + "/videos";

export const videoCourseApi = mainUrl + "/video-courses";

export const reviewApi = mainUrl + "/reviews";

export const api = mainUrl;
